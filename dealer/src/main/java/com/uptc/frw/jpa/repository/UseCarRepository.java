package com.uptc.frw.jpa.repository;

import com.uptc.frw.jpa.entity.UseCar;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UseCarRepository extends JpaRepository<UseCar, Long> {
}
